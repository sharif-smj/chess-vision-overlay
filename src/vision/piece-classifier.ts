// Classify pieces on each of the 64 squares
// Input: cropped board image → Output: FEN string
export class PieceClassifier {
  // TODO: Load ONNX model, classify each square
  async classify(boardImage: ImageData): Promise<string> {
    throw new Error('Not implemented — pending model selection (Issue #1)');
  }
}
